"""Generation helpers that compose retrieval + prompt to produce answers."""
from typing import Dict


def generate_answer(subgraph: Dict, question: str) -> str:
    """Simple placeholder for generating an answer from a retrieved subgraph."""
    return """This is a placeholder answer."""
