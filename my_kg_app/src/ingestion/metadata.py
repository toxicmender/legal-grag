"""Metadata extraction helpers (authors, dates, titles)."""

def extract_metadata(text: str) -> dict:
    # Placeholder: in practice use PDF metadata or NLP heuristics
    return {"title": None, "authors": [], "date": None}
